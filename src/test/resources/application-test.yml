spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/booking_test?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false
  sql:
    init:
      mode: always